window.Backbone ||= {}
window.Backbone.Views ||= {}

class Backbone.Views.<% if (typeof controllerName !== 'undefined'){ %><%= _(controllerName).classify() %><% } %><%= _(viewName).classify() %>View extends Backbone.View
  template: JST['<% if (typeof controllerName !== 'undefined'){ %><%= _(controllerName).underscored() %>/<% } %><%= _(viewName).underscored() %>']

  initialize: (options) ->
    @render()

  render: =>
    @$el.html(@template())

  onClose: ->
