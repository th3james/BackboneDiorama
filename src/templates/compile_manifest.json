[
  /*
   * This file is used to determine in what order your source files will
   * be compiled. Populate this it with your the location of your source
   * files (relative to `src/`). E.g.
   *
   *     'models/task',
   *     'collections/task'
   *
   * `diorama compile` will compile your Coffeescript and concantenate
   * the result into `js/application.js`.
   */
]
